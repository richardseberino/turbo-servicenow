(function executeRule(current, previous /*null when async*/) {
    var actionUtils = new x_turbo_turbonomic.TurbonomicActionUtils();
    actionUtils.updateApprovalBasedOnRequestStateChange(current);
})(current, previous);
